# Copy this file to .env and fill in values
# This file is used by docker compose at the project root

# Required for mcp-supabase service (Personal access token)
# Get it from Supabase Dashboard → Account → Access Tokens
SUPABASE_ACCESS_TOKEN=

# Required: Supabase Configuration
# Get from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=changeme-anon-key

# Optional: External access URL (for iOS/device testing)
# NEXT_PUBLIC_SUPABASE_URL_EXTERNAL=http://192.168.1.9:8000

# Service Role Key (for server-side operations that bypass RLS)
# Get from: Supabase Dashboard → Settings → API → service_role (secret)
SERVICE_ROLE_KEY=changeme-service-role-key
ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}

# Supabase Admin Configuration (for USDZ → GLB export pipeline)
# Service role key for server-side operations
SUPABASE_SERVICE_ROLE_KEY=${SERVICE_ROLE_KEY}

# Storage bucket name for USDZ/GLB files
BUCKET_NAME=exports

# Base URL for API calls (used by background functions)
# Set to your Vercel URL in production
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Required for MCP Supabase service
# Get from: Supabase Dashboard → Settings → General → Reference ID
# For local development, this can be left empty or set to a placeholder
SUPABASE_PROJECT_REF=

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🧪 Playwright / Smoke Test Credentials (optional)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Used by: scripts/dev-start.sh, e2e fixtures, and helper scripts
PLAYWRIGHT_ADMIN_EMAIL=
PLAYWRIGHT_ADMIN_PASSWORD=
PLAYWRIGHT_ARCHITECT_EMAIL=
PLAYWRIGHT_ARCHITECT_PASSWORD=
PLAYWRIGHT_END_USER_EMAIL=
PLAYWRIGHT_END_USER_PASSWORD=

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🗄️ PRODUCTION DATABASE CREDENTIALS (for dump/restore scripts)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Used by: scripts/dump-prod-to-local-docker.sh
PROD_DB_HOST=
PROD_DB_PORT=5432
PROD_DB_NAME=postgres
PROD_DB_USER=
PROD_DB_PASSWORD=




