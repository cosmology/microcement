// Centralized configuration for the project

export const config = {
  uploadedUrl: '/uploads',
};

export const paths = {
  upload: '/api/upload-image',
  home: '/',
  uploadPage: '/upload',
} as const; 